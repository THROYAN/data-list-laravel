export interface House {
    id: Number,
    attributes: {
        name: String,
        bedroomCount: Number,
        bathroomCount: Number,
        storeyCount: Number,
        garageCount: Number,
        price: Number,
    }
}
